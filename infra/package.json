{
  "name": "@alphalens/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.174.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "aws-cdk": "^2.174.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
